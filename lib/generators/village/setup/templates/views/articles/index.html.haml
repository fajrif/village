= render 'analytics'
= render 'feed_link'
%section#sidebar
  = render 'sidebar'

%section#articles
  - if collection.present?
    = render :partial => 'article', :collection => collection, :locals => { :summary => true }
    = paginate collection
  - else
    No articles found.